# 验收记录

## 2026-09-22：教练周计划、时长奖励、NetworkBunny

自动化验证：`npm test`、`npm run lint`、`npm run typecheck`、`npm run build`。

新增测试验证三档共用每日奖励上限、重复记录去重、失败不占次数、误判推翻后的重新分配、历史场次兼容、自定义奖励快照、不同训练日次数重置、教练计划权限和日期范围。API 测试验证客户端无法指定积分或奖励次数。

浏览器使用临时的本地隔离预览（已删除，未部署），复用正式页面组件与动作逻辑，不修改线上真实训练记录：

- 教练发布本周目标、投递与 Networking 重点、学习重点和验收标准；切到学员首页后正确显示。
- 学习模式显示同一周计划；目标可以多选，Networking 显示网站入口。
- 20 / 30 / 45 分钟与 15 / 25 / 40 积分显示正确；选 45 分钟时黑板倒计时同步变化。
- 390px 手机视口：新增目标、奖励选择和设置页无横向溢出。
- 教练奖励设置默认显示每日 5 次和 45 分钟 40 分；修改次数为 6 并保存后显示下一训练日生效提示。
- NetworkBunny 网站可打开；应用入口使用新标签页，不自动增加联系人数。

本次未开启摄像头或进行真实计时场次，没有将测试计划写入生产数据库。真实相机检测准确性和后台标签页长时间计时仍需在使用设备上体验。

## 2026-09-23：SQL 题的表结构与答题框同屏

自动化验证：`npm test`（145）、`npm run lint`、`npm run typecheck`、`npm run build`、`prettier --check`。

浏览器验证（本地隔离预览，未写入线上数据）：

- 桌面 1280px：数据表查阅区与答题框并排在同一屏，四张表和「答题约定」用标签切换；逐个切换标签并展开「思考提示」后，已写的 SQL 草稿保留，答题框位置没有移动。
- 键盘：标签用左右方向键切换，选中状态同步。
- 手机 390px：标签换成下拉选择，表格区域独立滚动，页面无横向溢出，草稿保留。
- 非 SQL 题（ML）：仍是原来的单栏卡片，思考提示在答题框上方，没有查阅区。

## 2026-09-23：投递记录里保存 JD

自动化验证：`npm test`（148，新增 3 条覆盖存储位置、权限、删除与长度上限）、`npm run lint`、`npm run typecheck`、`npm run build`、`prettier --check`。

浏览器验证（本地隔离预览，未写入线上数据）：

- 记录投递申请时粘贴 JD 并保存；训练记录里只有 `{at, chars}`，正文在 `job-docs` 桶里，state 中搜不到 JD 原文。
- 记录列表、打卡日历的当日记录、裁判「今日实况」都能点开 JD；弹窗显示全文、可滚动，并带职位链接。
- 「联系新朋友」的记录弹窗没有 JD 字段；演示模式下也不显示。
- 390px 手机视口：表单无横向溢出。
